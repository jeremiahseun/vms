{
    "name": "vws-engine",
    "version": "1.0.0",
    "private": true,
    "description": "Shared symlink and session engine for VWS",
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "test": "vitest run --config ../../vitest.config.ts --include 'src/**/*.test.ts'"
    },
    "dependencies": {
        "uuid": "^9.0.0"
    },
    "devDependencies": {
        "@types/uuid": "^9.0.0"
    }
}
